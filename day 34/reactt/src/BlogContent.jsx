function BlogContent() {
  return (
    <main>
      <p>
        React makes it easy to build reusable UI components using a
        component-based architecture.
      </p>
      <p>
        Nested components help keep code clean, readable, and maintainable.
      </p>
    </main>
  );
}

export default BlogContent;
